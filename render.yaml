services:
  - type: web
    name: telegram-forwarder
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python forwarder.py"
    envVars:
      - key: FORWARDER_BOT_TOKEN
        sync: false
